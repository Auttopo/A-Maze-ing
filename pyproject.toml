[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "A package for creating a maze"

[tool.setuptools.packages.find]
where = ["."]
include = ["mazegen"]